import { DescriptionShortnerPipe } from './description-shortner.pipe';

describe('DescriptionShortnerPipe', () => {
  it('create an instance', () => {
    const pipe = new DescriptionShortnerPipe();
    expect(pipe).toBeTruthy();
  });
});
